# Define the paths for the raw data, split data, and trained model
raw_data_dir = "data/raw" 
split_data_dir = "data/split"
save_model_path = "models/trained_model.pth"
saved_model_path = "models/trained_model.pth"

# model training hyperparameters
num_epochs = 10
learning_rate=0.001